<!DOCTYPE html>
<html>
<head>
    <title>Chi tiết đơn hàng</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <section class="h-100 h-custom" style="background-color: #eee;">
        <div class="container py-5 h-100">
            <div class="row d-flex justify-content-center align-items-center h-100">
            <div class="col-lg-8 col-xl-6">
                <div class="card border-top border-bottom border-3" style="border-color: #f37a27 !important;">
                <div class="card-body p-5">

                    <p class="lead fw-bold mb-5" style="color: #f37a27;">Dưới đây là chi tiết đơn hàng:</p>

                    <div class="row">
                    <div class="col mb-3">
                        <p class="small text-muted mb-1">Ngày</p>
                        <p>{{payment.date}}</p>
                    </div>
                    <div class="col mb-3">
                        <p class="small text-muted mb-1">Mã đơn hàng</p>
                        <p>{{payment.orderId}}</p>
                    </div>
                    </div>

                    <div class="mx-n5 px-5 py-4" style="background-color: #f2f2f2;">
                    <div class="row">
                        <p>Tên khách hàng: {{payment.name}}</p>
                    </div>
                    <div class="row">
                        <p>Ghi chú: {{payment.note}}</p>
                    </div>
                    <div>
                        <p>Số điện thoại: {{payment.telephone}}</p>
                    </div>
                    <div>
                        <p>Email: {{payment.email}}</p>
                    </div>
                    <div>
                        <p>Ngân hàng đã chọn: {{payment.cardType}}</p>
                    </div>
                    </div>

                    <div class="row my-4">
                    <div class="col-md-4 offset-md-8 col-lg-3 offset-lg-9">
                        <p class="lead fw-bold mb-0" style="color: #f37a27;">Tổng số tiền cần đáo hạn: {{payment.total}}vnd</p>
                    </div>
                    </div>
                </div>
                </div>
            </div>
            </div>
        </div>
        </section>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>